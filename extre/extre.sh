#!/bin/env/bash/extre
def void main(**char, **string $scanning)::[fi{
        let public string $URL = nmap.scanning.port(x)
}
fi
    let public string $scanning = "$URL"
    protect $scanning
    fi
]